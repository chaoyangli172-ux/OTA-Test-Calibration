# Phased-Array Automatic Calibration Tool

This repository contains a MATLAB-based automatic calibration tool for 256-element active phased-array antennas.

The program reads phase measurements obtained from an anechoic chamber, generates calibration codebooks, performs iterative phase calibration using circular statistics, and finally exports FPGA configuration files.  

<img width="379" height="88" alt="image" src="https://github.com/user-attachments/assets/a7179ac1-947a-412a-96d6-c068d2b35bd8" />  

*Figure 1. imported test data .txt criterion for MATLAB regular expressing match*



<img width="547" height="500" alt="image" src="https://github.com/user-attachments/assets/37a93e75-0b50-446c-941a-6605de83298a" />  

*Figure 2. exported FPGA code example that could be copied and used by FPGA enginners directly*

---

## Features

- Automatic processing of measured phase data
- Support for dual-polarized phased arrays
- Circular-statistics-based flatness evaluation
- Multi-frequency calibration
- Automatic FPGA initialization code generation
- Multiple calibration iteration

---

## Usage Guide
1. **OTA_Calibration_code.m** is the baseline code, in which the array mode is fixed, such as SPI orders and antenna mirror mode generated by sequential rotation and power divider.  
2. In order to solve the non-univerality problem, **OtaMapCalibrationApp.m** has been developed. Running it, the figure below will be shown, which provides enough degrees of freedom for OTA test.  
<img width="1412" height="600" alt="image" src="https://github.com/user-attachments/assets/78fe5ca7-f5c1-4b81-b04c-a1f110bc9e6f" />  
*Figure 3. App window*  
3. Download **OtaMapCalibrationApp.m** with the other five png files in this repository. The png files can teach you the order and mode meanings in Figure 3.  


## Calibration Workflow

1. Import measured data as required criterions at given files.  
2. Input the phase shifter HEX codebook.  
3. Fill in frequency and array parameters based on the image examples.  
4. Select array and SPI mode based on the image examples.  
5. Click **Run Calibration** button and get .txt file where fpga code can be copied and pasted directly.  
6. Do OTA test again and acquire the next measured data. Import new data at given files.  
7. Select **Calibration pass** to the next number.  
8. Return to 6th step until flatness.  

---

## Calibration Criterion

The calibration quality is evaluated using the circular standard deviation

σ < 3°

Once all operating frequencies satisfy this requirement, the calibration process terminates automatically.

---


## Input

The input data should be organized as

```
testdata[number of calibration times]/

    vertical/

        1.txt

        2.txt

        ...

    horizontal/

        1.txt

        2.txt

        ...
```

---

## Output

The program automatically generates

- Updated calibration codebooks (.xlsx)
- FPGA initialization files (.txt)

---

## Design Highlights

The calibration algorithm

- evaluates phase consistency using circular statistics;
- automatically skips frequency points that have already converged;
- performs independent calibration for V and H polarizations;
- iteratively updates phase-shifter control codes until the required calibration accuracy is achieved.

---

## Notes

This repository contains the complete implementation used for automatic phase calibration.

Some project-specific hardware interfaces and measurement files are omitted.
